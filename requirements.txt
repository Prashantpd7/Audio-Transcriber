# Audio Transcriber — Python Dependencies
#
# Install:
#   pip install -r requirements.txt

# Core transcription engine
faster-whisper>=1.0.0

# Audio decoding (handles MP3, MP4, WAV, M4A, AAC, FLAC, OGG, WEBM)
# Replaces the need for a system-level FFmpeg installation.
av>=12.0.0

# Numerical computing (required by faster-whisper & audio loading)
numpy>=1.24.0

# Live microphone capture (real-time transcription)
sounddevice>=0.5.0
