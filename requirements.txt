# Audio Transcriber — Python Dependencies
#
# Install:
#   pip install -r requirements.txt

# Core transcription engine
faster-whisper>=1.0.0

# Note: FFmpeg is required for audio processing but is a system-level
# dependency installed via your package manager (brew, apt, choco, etc.).
# See README.md for installation instructions.
